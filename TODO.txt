Собрать docker file из приложения, оно должно запускать тесты при запуске контейнера, затем отправлять результаты тестов из дериктории build/allure-result на сервер allure (который надо установить).

Соединить в Docker compose.

Написать инструкцию как это запустить в readme

Залить в репо.